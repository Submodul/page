{
  "name": "fix",
  "version": "1.0.0",
  "description": "fix",
  "scripts": {
    "dev": "bundle exec jekyll serve --livereload",
    "yml": "ruby yml.rb",
    "svg-png": "ruby svg-png.rb",
    "tost": "rm -rf _data/services && mkdir _data/services && rm -rf _services && mkdir _services && ruby tost.rb",
    "git": "git add . -A && git commit -a -m \"update\" && git push"
  },
  "author": "",
  "license": "ISC",
  "homepage": "",
  "devDependencies": {
    "gulp-clean-css": "^4.3.0",
    "gulp-minify": "^3.1.0",
    "gulp-minify-selectors": "^1.1.0",
    "gulp-v3": "^3.12.1"
  }
}
